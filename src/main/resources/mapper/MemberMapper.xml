<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.MemberDao">

    <select id="memberList" resultType="Member">
        SELECT *
          FROM member
    </select>

    <insert id="memberInst" parameterType="Member">
        INSERT INTO member ( mmName
                           , mmGender
                           )
                    VALUES ( #{mmName}
                           , #{mmGender}
                           )
    </insert>

    <select id="selectOneForName" resultType="Member">
        SELECT mmSeq
             , mmName
             , mmGender
             , mmCreateDate
          FROM member
         WHERE mmName LIKE CONCAT('%', #{mmName}, '%')
    </select>
</mapper>